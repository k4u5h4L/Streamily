import React from "react";

const SongListOver = () => {
    return (
        <div className="text-center">
            <h3 className="font-weight-bold text-black">That's all for now!</h3>
        </div>
    );
};

export default SongListOver;
